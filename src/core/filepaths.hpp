/**
 * @file filepaths.hpp
 *
 * @brief Filepaths to assets used by the application.
 */

#pragma once

#include <string>  // for std::string

namespace core {
namespace filepaths {

// /**
//  * @brief Path to the state JSON file (contains learning state).
//  */
// extern const std::string state;

/**
 * @brief Path to the vocabulary JSON file (contains Japanese words).
 */
extern const std::string vocabulary;

}  // namespace filepaths
}  // namespace core
